<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Back Office</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item {% if app.current_route|slice(0,7) == 'app_car' %} active {% endif %}">
        <a class="nav-link" href="{{path('app_car_browse')}}">Voitures<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item {% if app.current_route|slice(0,9) == 'app_brand' %} active {% endif %}">
        <a class="nav-link" href="{{path('app_brand_browse')}}">Marques</a>
      </li>
    </ul>
  </div>
</nav>